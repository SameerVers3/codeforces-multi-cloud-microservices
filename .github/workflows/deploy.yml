name: Build and Deploy

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

permissions:
  contents: read
  packages: write
  security-events: write

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/codeforces-multi-cloud-microservices

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        service: [auth-service, contest-service, submission-service, execution-service, scoring-service, leaderboard-service]
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Convert actor to lowercase for Docker registry
      id: repo
      run: |
        echo "owner=$(echo '${{ github.actor }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
    
    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      id: docker
      with:
        context: ./services/${{ matrix.service }}
        push: ${{ github.event_name != 'pull_request' }}
        # Use user packages (ghcr.io/USERNAME/package) instead of org packages to avoid permission issues
        tags: ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/codeforces-${{ matrix.service }}:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64
    
    - name: Run security scan
      uses: aquasecurity/trivy-action@master
      if: github.event_name != 'pull_request' && steps.docker.outcome == 'success'
      id: trivy-scan
      continue-on-error: true
      with:
        image-ref: ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/codeforces-${{ matrix.service }}:${{ github.sha }}
        format: 'sarif'
        output: 'trivy-results-${{ matrix.service }}.sarif'
        exit-code: '0'
    
    - name: Check if Trivy results exist
      id: check-trivy
      if: github.event_name != 'pull_request' && steps.trivy-scan.outcome != 'failure'
      run: |
        if [ -f "trivy-results-${{ matrix.service }}.sarif" ]; then
          echo "exists=true" >> $GITHUB_OUTPUT
        else
          echo "exists=false" >> $GITHUB_OUTPUT
        fi
    
    - name: Upload Trivy results to GitHub Security
      uses: github/codeql-action/upload-sarif@v4
      if: github.event_name != 'pull_request' && steps.check-trivy.outputs.exists == 'true'
      with:
        sarif_file: 'trivy-results-${{ matrix.service }}.sarif'
        wait-for-processing: false

  build-frontend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
    
    - name: Convert actor to lowercase for Docker registry
      id: repo
      run: |
        echo "owner=$(echo '${{ github.actor }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT
    
    - name: Log in to Container Registry
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Build and push frontend Docker image
      uses: docker/build-push-action@v4
      with:
        context: ./frontend/nextjs-app
        push: ${{ github.event_name != 'pull_request' }}
        tags: ${{ env.REGISTRY }}/${{ steps.repo.outputs.owner }}/codeforces-frontend:${{ github.sha }}
        cache-from: type=gha
        cache-to: type=gha,mode=max
        platforms: linux/amd64

  test:
    runs-on: ubuntu-latest
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_USER: codeforces
          POSTGRES_PASSWORD: codeforces_test
          POSTGRES_DB: codeforces_test_db
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        cd services/auth-service
        pip install -r requirements.txt
        pip install pytest pytest-cov
    
    - name: Initialize test database
      env:
        DATABASE_URL: postgresql://codeforces:codeforces_test@localhost:5432/codeforces_test_db
        PYTHONPATH: ${{ github.workspace }}/services/auth-service
      run: |
        cd services/auth-service
        python -c "from app.database import init_db; init_db()"
    
    - name: Run tests
      env:
        DATABASE_URL: postgresql://codeforces:codeforces_test@localhost:5432/codeforces_test_db
        PYTHONPATH: ${{ github.workspace }}/services/auth-service
      run: |
        cd services/auth-service
        pytest tests/ -v --cov=app --cov-report=xml
    
    - name: Upload coverage
      uses: codecov/codecov-action@v3
      with:
        file: ./services/auth-service/coverage.xml

  deploy:
    needs: [build, build-frontend, test]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.5.0
    
    - name: Terraform Init
      run: |
        cd infrastructure/terraform/aws && terraform init
        cd ../azure && terraform init
        cd ../gcp && terraform init
    
    - name: Terraform Plan
      run: |
        cd infrastructure/terraform/aws && terraform plan -out=tfplan
        cd ../azure && terraform plan -out=tfplan -var="db_admin_login=${{ secrets.DB_ADMIN_LOGIN }}" -var="db_admin_password=${{ secrets.DB_ADMIN_PASSWORD }}"
        cd ../gcp && terraform plan -out=tfplan -var="gcp_project_id=${{ secrets.GCP_PROJECT_ID }}"
    
    - name: Terraform Apply
      if: github.ref == 'refs/heads/main'
      run: |
        cd infrastructure/terraform/aws && terraform apply -auto-approve tfplan
        cd ../azure && terraform apply -auto-approve tfplan -var="db_admin_login=${{ secrets.DB_ADMIN_LOGIN }}" -var="db_admin_password=${{ secrets.DB_ADMIN_PASSWORD }}"
        cd ../gcp && terraform apply -auto-approve tfplan -var="gcp_project_id=${{ secrets.GCP_PROJECT_ID }}"
    
    - name: Deploy to Kubernetes
      run: |
        # Deploy to AWS EKS
        aws eks update-kubeconfig --name codeforces-execution-cluster --region us-east-1
        kubectl apply -f infrastructure/kubernetes/base/
        kubectl apply -f infrastructure/kubernetes/network-policies.yaml
        kubectl apply -f infrastructure/kubernetes/rbac.yaml
        
        # Deploy to Azure AKS
        az aks get-credentials --resource-group codeforces-rg --name codeforces-aks-cluster
        kubectl apply -f infrastructure/kubernetes/base/
        kubectl apply -f infrastructure/kubernetes/network-policies.yaml
        kubectl apply -f infrastructure/kubernetes/rbac.yaml
        
        # Deploy to GCP GKE
        gcloud container clusters get-credentials codeforces-gke-cluster --region us-central1
        kubectl apply -f infrastructure/kubernetes/base/
        kubectl apply -f infrastructure/kubernetes/network-policies.yaml
        kubectl apply -f infrastructure/kubernetes/rbac.yaml

