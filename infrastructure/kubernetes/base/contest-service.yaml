apiVersion: apps/v1
kind: Deployment
metadata:
  name: contest-service
  namespace: codeforces
  labels:
    app: contest-service
    cloud: azure
spec:
  replicas: 2
  selector:
    matchLabels:
      app: contest-service
  template:
    metadata:
      labels:
        app: contest-service
        cloud: azure
    spec:
      serviceAccountName: codeforces-service-account
      containers:
      - name: contest-service
        image: ghcr.io/sameervers3/codeforces-contest-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8000
          name: http
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: db-secret
              key: url
        - name: REDIS_URL
          valueFrom:
            secretKeyRef:
              name: redis-secret
              key: url
        - name: AUTH_SERVICE_URL
          value: "http://auth-service.codeforces.svc.cluster.local:80"
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: contest-service
  namespace: codeforces
  labels:
    app: contest-service
spec:
  selector:
    app: contest-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
  type: LoadBalancer

